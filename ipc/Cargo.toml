[package]
name = "ipc"
version.workspace = true
edition.workspace = true

[dependencies]
leandros-lib = { path = "../lib" }
sched   = { path = "../sched", optional = true }
spin.workspace = true

[features]
default = ["kernel"]
kernel = ["dep:sched"]
