[package]
name = "leandros-greeter-launch"
version.workspace = true
edition.workspace = true

[[bin]]
name = "greeter-launch"
path = "src/main.rs"

[dependencies]
leandros-libc = { path = "../libc" }
