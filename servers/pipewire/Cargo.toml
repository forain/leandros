[package]
name = "pipewire-server"
version.workspace = true
edition.workspace = true

[dependencies]
sched = { path = "../../sched" }
drivers = { path = "../../drivers" }
ipc = { path = "../../ipc" }
vfs-server = { path = "../vfs" }
net-server = { path = "../net" }
spin.workspace = true
