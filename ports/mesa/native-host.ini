# meson --native-file: lets the BUILD machine (macOS) pkg-config find the
# native wayland-scanner.pc during a cross build. env PKG_CONFIG_PATH is
# IGNORED for native:true deps in a cross build, so this file is required.
[built-in options]
pkg_config_path = ['@DIRNAME@/host/lib/pkgconfig']
