diff --git a/greetd/Cargo.toml b/greetd/Cargo.toml
index 7d471ae..608256b 100644
--- a/greetd/Cargo.toml
+++ b/greetd/Cargo.toml
@@ -13,6 +13,9 @@ debug = []
 [dependencies]
 nix = { version = "0.28", features = ["ioctl", "signal", "user", "fs", "mman"] }
 pam-sys = "0.5.6"
+# LeandrOS has no PAM stack. This crate supplies the libpam application ABI
+# pam-sys binds to, backed by /etc/shadow. See ports/greetd/pam-leandros.
+pam-leandros = { path = "../../pam-leandros" }
 serde = { version = "1.0", features = ["derive"] }
 serde_json = "1.0"
 greetd_ipc = { path = "../greetd_ipc", features = ["tokio-codec"] }
diff --git a/greetd/src/main.rs b/greetd/src/main.rs
index e66fdf1..78bd5bf 100644
--- a/greetd/src/main.rs
+++ b/greetd/src/main.rs
@@ -20,6 +20,11 @@ use tokio::task;
 
 use crate::{error::Error, session::worker};
 
+// Linked for its symbols, not its API: pam-leandros defines the pam_* C ABI
+// that pam-sys declares as extern. Without a reference the crate is never
+// linked and every pam_* call becomes an undefined symbol.
+use pam_leandros as _;
+
 async fn session_worker_main(config: config::Config) -> Result<(), Error> {
     let raw_fd = config.internal.session_worker as RawFd;
     let mut cur_flags = FdFlag::from_bits_retain(fcntl(raw_fd, FcntlArg::F_GETFD)?);
diff --git a/greetd/src/session/interface.rs b/greetd/src/session/interface.rs
index baed428..bd722c8 100644
--- a/greetd/src/session/interface.rs
+++ b/greetd/src/session/interface.rs
@@ -1,6 +1,6 @@
 use std::{
     ffi::CString,
-    os::unix::{io::AsRawFd, net::UnixDatagram},
+    os::unix::{ffi::OsStringExt, io::AsRawFd, net::UnixDatagram},
 };
 
 use nix::{
@@ -108,12 +108,28 @@ impl Session {
         // in spite of unlink or rename similar to an already open fd. It is, however, not very
         // portable. On FreeBSD, we would likely need to open our own process file early and rely
         // on fexecve, using the KERN_PROC_PATHNAME sysctl to get the path.
+        // Resolve /proc/self/exe before the fork rather than handing the magic
+        // path to execv. LeandrOS synthesises /proc/self/exe inside readlink(2)
+        // only — it has no VFS node — so execv on the literal path fails with
+        // ENOENT. current_exe is that readlink, and what it returns is the
+        // absolute path the kernel recorded at our own execve, so it is
+        // directly re-executable. Resolving in the parent also turns a failure
+        // into a returned error instead of a panic in a forked child that has
+        // no way to report one.
+        let self_exe = CString::new(
+            std::env::current_exe()
+                .map_err(|e| format!("unable to resolve own executable: {e}"))?
+                .into_os_string()
+                .into_vec(),
+        )
+        .map_err(|e| format!("own executable path is not a valid C string: {e}"))?;
+
         #[allow(unreachable_code)]
         let child = match unsafe { fork() }.map_err(|e| format!("unable to fork: {e}"))? {
             ForkResult::Parent { child, .. } => child,
             ForkResult::Child => {
                 execv(
-                    &CString::new("/proc/self/exe").unwrap(),
+                    &self_exe,
                     &[
                         &CString::new("greetd").unwrap(),
                         &CString::new("--session-worker").unwrap(),
