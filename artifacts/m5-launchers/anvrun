mkdir -p /run/user/0
export ANVIL_DRM_DEVICE=/dev/dri/card0
export SMITHAY_USE_LEGACY=1
export XDG_RUNTIME_DIR=/run/user/0
export RUST_LOG=info
unset DISPLAY
unset WAYLAND_DISPLAY
echo ANVRUN-START
anvil --tty-udev
echo ANVRUN-END-RC $?
